# Questions

One file per system design question actually tackled (e.g. `url-shortener.md`,
`rate-limiter.md`). Each note covers requirements, the approach discussed, trade-offs,
and mistakes made — never a full polished design copy-pasted from elsewhere.
