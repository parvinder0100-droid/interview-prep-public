# Concepts

One file per system design building block, created only when actually covered in a
session (e.g. `load-balancing.md`, `caching.md`, `sharding.md`, `consistency-models.md`).
No pre-loaded curriculum — this folder starts empty by design.
